<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.beautyshop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DisplayFullProduct"></activity>
        <activity android:name=".Display_prowithimg" />
        <activity android:name=".SplashActivty">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".admin_insert_product_page">



        </activity>
        <activity android:name=".Display_imgex" />
        <activity android:name=".ex_img" />
        <activity android:name=".sign_act" />
        <activity android:name=".Login" />
        <activity android:name=".ProductDisplay" />
        <activity android:name=".insert_userform" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>